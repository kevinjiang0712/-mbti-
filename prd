1. 背景与目标
1.1 背景
面向“家里养猫、把猫当孩子”的年轻用户，通过可观察行为题库，为猫咪生成 16 型人格（CBTI），输出标签化结果与简短描述，满足“给猫贴标签”的娱乐与情绪价值需求。
1.2 MVP目标（上线可用）
- 3 个页面跑通闭环：首页 → 测试页 → 结果页
- 全部本地数据（题库、结果库、计分逻辑），不接后端、不接分享、不做埋点。
1.3 非目标（MVP不做）
- 分享海报、转发、好友互动
- AIGC 图片生成/上传猫照片
- 账号体系、云开发存储、历史记录
- 数据埋点、A/B、商业化、订阅、广告

---
2. 用户与使用场景
2.1 目标用户
- 18–35 养猫人群，偏“情感投射/拟人化”用户
- 单身/不婚倾向用户（非必须，但契合）
2.2 核心场景
用户打开小程序，用 1–2 分钟完成测试，获得“我家猫是什么人格类型”的结论与简短解读。

---
3. 核心体验与信息架构
3.1 页面结构
- 首页（Home）
- 测试页（Quiz）
- 结果页（Result）
3.2 关键路径
进入小程序 → 点击开始测试 → 逐题选择 → 提交 → 输出结果

---
4. 功能需求（按页面）
4.1 首页（Home）
4.1.1 功能点
- 展示产品标题与一句话价值主张
  - 标题：猫咪 CBTI 人格测试
  - 副标题：用 12 个问题测出你家猫的“人格类型”
- 主 CTA 按钮：开始测猫
- 可选：简短提示（不影响流程）
  - “请按猫在多数情况下的表现选择”
4.1.2 交互
- 点击“开始测猫”进入测试页，默认从第 1 题开始，清空上次测试状态。
4.1.3 异常
- 无。

---
4.2 测试页（Quiz）
4.2.1 功能点
- 显示当前题目（单选）
- 显示进度：当前题号 / 总题数
- 选项选择后自动进入下一题（减少操作）
- 最后一题选择后，显示“查看结果”按钮（或自动跳转结果页）
4.2.2 数据与规则
- 题目数量：12 题（MVP固定）
- 每题两个选项，每个选项对应一个维度得分（E/I、S/N、T/F、J/P）
- 维度题目分配：每个维度 3 题
- 出结果规则：某维度某字母得分 ≥ 2 即取该字母，拼成 4 字母类型（例：INTJ）
4.2.3 状态管理
- currentIndex：当前题索引
- scores：四维累计分数结构，例如
  - E, I, S, N, T, F, J, P（初始化为 0）
- answers（可选）：记录每题选择，用于返回上一题（MVP可不做返回）
4.2.4 交互细节
- 单选点击即选中（高亮）并：
  - 若非最后一题：currentIndex + 1
  - 若为最后一题：展示“查看结果”按钮
- “查看结果”跳转到结果页，并传递计算出的 type
4.2.5 异常处理
- 用户未选择就试图下一步：MVP无“下一步”按钮则天然避免；若保留按钮则需 toast 提示“请选择一个选项”。

---
4.3 结果页（Result）
4.3.1 功能点
- 展示结果类型与人格称号
  - 例：INTJ 冷面霸总猫
- 展示标签（3–5 个）
- 展示简短描述（100–140 字）
- 提供 2 个按钮：
  - “再测一次”（回到测试页，清空状态）
  - “返回首页”（回到首页）
4.3.2 数据
- resultsMap：16 型结果文案映射（本地静态 JSON/TS 文件）
4.3.3 异常
- 未拿到 type 或映射缺失：
  - 默认 fallback：显示“未识别类型”，提供“再测一次”。

---
5. 计分与类型计算（功能规格）
5.1 计分键值（建议）
- 使用统一 scoreKey：E/I/S/N/T/F/J/P
- 每选择一次对相应键 +1
5.2 类型计算算法（确定性）
- EI：E >= 2 ? 'E' : 'I'
- SN：S >= 2 ? 'S' : 'N'
- TF：T >= 2 ? 'T' : 'F'
- JP：J >= 2 ? 'J' : 'P'
- type = EI + SN + TF + JP

---
6. 内容资产（MVP范围）
- 题库：12 题（固定题库）
- 结果库：16 型（固定文案）

---
7. 视觉与交互要求（MVP最低标准）
- 三段式布局：标题区 / 内容区 / 按钮区
- 测试页选项点击反馈明确（高亮、勾选态）
- 结果页信息层级清晰：类型 > 称号 > 标签 > 描述
- 全程无登录、无授权弹窗